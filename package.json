{
  "name": "merenda",
  "private": true,
  "version": "1.0.0",
  "description": "Merenda — menu & ordering platform: client site, admin CMS, Go API",
  "workspaces": [
    "packages/*",
    "frontend",
    "admin"
  ],
  "scripts": {
    "dev:site": "npm run dev -w frontend",
    "dev:admin": "npm run dev -w admin",
    "build": "npm run build -w packages/shared && npm run build -w frontend && npm run build -w admin",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w frontend && npm run typecheck -w admin",
    "lint": "npm run lint -w frontend && npm run lint -w admin"
  },
  "engines": {
    "node": ">=20"
  }
}
