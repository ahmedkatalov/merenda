# Leave empty to call the API same-origin (dev server proxies /api to the backend).
VITE_API_URL=
# Backend used by the Vite dev proxy
VITE_DEV_API_URL=http://localhost:8080
# Public site URL, embedded in the live preview iframe
VITE_SITE_URL=http://localhost:5173
